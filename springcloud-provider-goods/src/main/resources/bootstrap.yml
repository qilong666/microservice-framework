






spring:
  application:
    name: springcloud-provider-goods
  cloud:
    config:
      lable: master
      profile: dev
      discovery:
        enabled: true
        service-id: springcloud-config-server
      uri: http://localhost:8090